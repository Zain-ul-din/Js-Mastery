// Node packages : ` npm init ` to create own package

console.log('Packages')

// npm install --help
// npm info <packagename>
// npm install <packagename> || npm i <packagename> 
// npm uninstall <packagename>

// npx yarn add <packagename>

// Flags --save # saves package meta data
   // -g | npm i yarn -g 


// version 1.0.0 major(gaint changes).minor(bug fix or new Changes).patch(some changes in docs)

// ` scripts ` in ` package.json `

// add .gitignore
// node_modules


// import module from 'mypackage' | telling node it's going to be internal module or module we have installed

